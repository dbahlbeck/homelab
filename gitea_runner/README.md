# Gitea Runner

Deploys a stack with a Gitea runner.

You'll need to environment variables, that must be set in the Portainer UI:

`GITEA_RUNNER_REGISTRATION_TOKEN`: The runner registration token from Gitea.
`GITEA_INSTANCE_URL`: The URL of the Gitea instance