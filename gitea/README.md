# Gitea Notes

Prepare an external volume in Portainer before deploying. The container should be called `gitea-data`.

## Adding a runner

To deploy the runner stack after this, complete the setup in the browser and use the runner token 
when creating the runner stack.